export const LAYOUT_LIMITS = {
  // Shared fallback limits for legacy/simple panel helpers.
  leftPanel: {
    min: 180,
    max: 500,
  },
  rightPanel: {
    min: 220,
    max: 600,
  },
  console: {
    min: 100,
    max: 400,
  },
};
